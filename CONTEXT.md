# CONTEXT

羽毛球订场系统的领域术语表。只记录领域语言，不记录实现细节。

## 核心概念

### 辅助订场 (Booking Intent)

用户声明的"我想在某个时间打到球"的需求。系统的中心概念：用户表达意图（日期/星期、时间段、场地偏好），系统自动完成后续的监控与锁场。监控和锁场不是用户需要理解的概念，只是意图的执行手段。界面上称为"辅助订场"。

旧代码中的 "订场监控目标 (venue_watch_target)" 即为此概念的前身，但旧模型把监控当作中心、锁场当作附属开关，与用户的真实心智不符。

意图是**长期有效的站位指令**：启用期间每逢设定时间就监控并锁场；**整段锁到后意图自动停用**（锁到即停），用户判断要不要支付——不支付就手动重开意图继续监控，支付了本就不再需要；部分锁到（时长没凑齐）不停用，继续监控剩余小时。单次日期意图在日期过后自然过期。

### 放票窗口 (Booking Window)

体育馆每天 09:00 放出"今天起第 4 天"的场次，即任何时刻只有未来 4 天（含今天）可订。因此即使一周 7 天各有一个意图，同一时刻最多 4 个意图处于可订窗口内。

### 可订时段 (Bookable Hours)

场馆每天实际可订的时段范围是 09:00-21:00（最后一个时段 20:00-21:00），按小时划分。时段之外的时间（早上 9 点前、晚上 9 点后）场馆不开放预订。分时段定价：白天（09:00-17:00）¥35/小时，晚间（17:00-21:00）¥45/小时。

### 场地 (Area)

体育馆内一片可独立预订的羽毛球场（如 1 号场、2 号场）。外部体育馆系统中的最小可订单元，由 `uniq_no` 唯一标识一个"某场地 × 某时段"的可订格子。

### 场地优先级 (Area Priority)

用户对多个场地的偏好排序（如 3 号场 > 5 号场 > 任意），锁场时按此顺序尝试。属于用户偏好，可在意图上覆盖全局默认。

### 锁场 (Auto-Lock)

系统代替用户在外部体育馆小程序自动创建**未支付订单**，把场地时段占住。订单 5 分钟内不支付会自动取消，且只能在微信客户端内完成支付。

锁场相关规则：

- **抢失败不放弃**：锁场时被他人抢先，系统继续盯，格子再次可订时再抢，直到该时段过去。
- **不跟踪支付**：锁场成功（订单创建）即视为成功，系统不关心支付是否完成，也没有接口可查。锁场记录只有两种状态：成功 / 失败。格子再次可订时仅把记录标记为"已释放"（用于每日限订额度记账），不重锁、不推送。
- **唯一带签名的接口**：全链路上只有 `createOrder` 要求 `X-Ca-Timestamp` / `X-Ca-Nonce` / `X-Ca-Signature`（阿里云 API 网关风格，RSA-2048 SHA256withRSA）。签名算法与常量见 `server/src/services/venueLockSigner.js` 头部注释；私钥由 `.env` 的 `GYM_SIGN_PRIVATE_KEY` 提供（PKCS#8 PEM）。未配置时锁场链路优雅降级为只推送可订提醒（`SignNotConfiguredError`）。

### 放票风控 (Release Risk Control)

每天 09:00 放票高峰，外部系统会对 createOrder 触发**图形验证**（选择相似图形），账号级拦截，自动下单无法通过。系统无法自行过验证（ticket 只能由微信客户端产生），改为**引导用户配合 + 窗口重试**：

- 首次被拦（不逐个场地递补）即推「需要过验证」，请用户在小程序里点一次「预订」拿到验证、看到"验证成功"就退出（不用真下单）。
- 服务端与小程序**共用同一 token-user**，用户过一次验证后，服务端请求按会话免验证。
- 随后进入 **4 分钟重试窗口**（每 12 秒一轮）：重新拉取当日场次并绕过 diff 直接评估该意图，锁到即推「已锁场」。
- 停止条件：锁到 / 意图被停用或删除 / 总开关关闭 / token 失效（告警交主流程）/ 窗口超时（推「锁场失败」，提示可手动订、回流照常）。
- 同一意图同一天只会有一个重试窗口在跑，不叠加。

窗口与间隔是经验参数，在 `server/src/services/watchEngine.js` 的 `rcRetryConfig`（默认 `{ intervalMs: 12000, windowMs: 4 * 60 * 1000 }`）。非放票时刻的回流场次不触发该验证，自动锁场正常生效。

### 每日限订 (Daily Order Limit)

场馆限制每账号**每天最多下 2 笔订单**（按下单日计算，与订哪天的场无关；未来可能放宽到 3 笔，由服务器 `GYM_DAILY_ORDER_LIMIT` 环境变量配置）。未支付超时取消的订单**返还额度**。系统本地按"当前持有中的锁场订单数"预估额度（格子回流时标记"已释放"），额度不足整段需求时当天停手并推送告知；外部接口的限订报错作为兜底。

### 时段 (Slot)

场地上一个可预订的时间格子（如 19:00-20:00），时长固定为 1 小时。

### 时间窗口 (Time Window)

意图中"我哪个时间段有空"的范围（如 17:00-20:00，或一整天）。窗口只是候选范围，不代表要打满整个窗口。

### 打球时长 (Duration)

意图中"我想打几个小时"。系统需要在时间窗口内找到**连续的** N 个小时时段（N = 时长）并锁到，意图才算满足。用户不接受中间隔开几小时打两场。例如窗口 17:00-20:00、时长 2 小时，则 17-19 或 18-20 都是满足，17-18 加 19-20（断开）不是。连续的几个小时**允许跨场地**（如 17-18 在 3 号场、18-19 在 5 号场）。
