name: badmintontest

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_DIR: dist-test
    container_name: badminton-test
    restart: unless-stopped
    env_file:
      - server/.env
    ports:
      - "8090:3000"
    volumes:
      - ./server/database:/app/server/database
      - ./server/uploads:/app/server/uploads
      - ./server/runtime:/app/server/runtime
    environment:
      - NODE_ENV=production
      - PORT=3000
      - TZ=Asia/Shanghai
      - DB_PATH=/app/server/database/test.db
      - ADMIN_TOKEN=${ADMIN_TOKEN:-}
      - ENABLE_TEST_FEATURES=true
